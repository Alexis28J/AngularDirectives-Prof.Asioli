import { SuperButton } from './super-button';

describe('SuperButton', () => {
  it('should create an instance', () => {
    const directive = new SuperButton();
    expect(directive).toBeTruthy();
  });
});
