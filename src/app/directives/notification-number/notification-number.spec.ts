import { NotificationNumber } from './notification-number';

describe('NotificationNumber', () => {
  it('should create an instance', () => {
    const directive = new NotificationNumber();
    expect(directive).toBeTruthy();
  });
});
